[pytest]
testpaths = tests
asyncio_mode = auto
log_cli = true
log_cli_level = INFO
markers =
    integration: Integration tests that require full gateway stack (MockAdapter, SystemManager, optional Ollama)
