# AgriMeshAI — Python Dependencies
# Install: pip install -r requirements.txt

# MCP Gateway (agrimesh)
pydantic>=2.0
click>=8.0
aiosqlite>=0.20
mcp>=1.0
pyserial>=3.5
pyserial-asyncio>=0.6
paho-mqtt>=2.0

# AI Agent (edge-agent)
openai>=1.0
httpx>=0.27
pyyaml>=6.0

# TOML parser (Python <3.11)
tomli>=2.0
